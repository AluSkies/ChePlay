package com.cheplay.config;

/**
 * Placeholder config removed to avoid duplicate bean definitions when using the
 * application's actual Neo4j configuration in org.cheplay.neo4j.Neo4jConfig.
 *
 * Left as a deprecated placeholder (no @Configuration) to preserve package layout.
 */
@Deprecated
public class Neo4jConfig {
	// Intentionally empty. Real configuration lives in org.cheplay.neo4j.Neo4jConfig
}